# Migrations

Store new versioned upgrade files here using the pattern `upgrade_vNN.sql`.
After adding a migration, regenerate the consolidated SQL entry files with `php scripts/db/rebuild_consolidated_sql.php`.
