# Database files

## Recommended files
- `schema.sql` — consolidated schema for fresh installs
- `seed_demo.sql` — optional demo seed template
- `legacy_upgrade_bundle.sql` — convenience bundle reference for old installs

## Legacy files
The `upgrade_v*.sql` files are kept for backward compatibility and patch history. New environments should prefer `schema.sql`.
